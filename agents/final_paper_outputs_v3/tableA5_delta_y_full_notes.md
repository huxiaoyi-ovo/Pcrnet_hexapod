# Table A5 notes

- Risk-only values are aggregated from the trained three-seed main evaluation.
- Learned-w values use the current seed-1 mechanism diagnostic because the older diagnostic predates the C_unsafe component split.
- Each row reports components from one internally consistent source; Delta y_total is not used to back-calculate missing components.
